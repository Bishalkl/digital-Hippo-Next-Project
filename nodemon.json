{
  "watch": ["srver.ts", "src/collections/**/*.ts", "src/trpc/index.ts"],
  "exect": "ts-node --project tsconfig.server.json src/server.ts -- -I",
  "ext": "js ts",
  "stdin": false,
  "scripts": {
    "start:prod": "next build && cross-env PAYLOAD_CONFIG_PATH=src/payload.config.ts nodemon"
  }
}
